# Fotografía

Coloca aquí tus imágenes (01.jpg, 02.jpg, …) y decláralas en `lib/gallery.ts`
(array `photos`). Indica `width`/`height` aproximados de cada foto para que la
galería reserve el aspecto correcto y no haya saltos de layout.
