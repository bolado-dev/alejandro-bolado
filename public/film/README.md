# Filmmaking

- Vídeos locales: coloca los .mp4 aquí y referencia su ruta en `lib/gallery.ts`
  (campo `src`, p.ej. "/film/01.mp4"). Añade una miniatura `*-poster.jpg`.
- O usa YouTube/Vimeo: rellena el campo `embed` con la URL embebida.
- `showreel` es el vídeo destacado grande de la sección.
